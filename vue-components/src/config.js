'use strict';

import manifest from './manifest.json';

const config = {
  appNamespace: 'demo-issuer',
  manifest,
  Quasar: {
    brand: {
      primary: '#29A19C'
    }
  }
};

export default config;
